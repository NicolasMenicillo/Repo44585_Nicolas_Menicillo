{
  "name": "refrigeracion_nm",
  "version": "1.0.0",
  "description": "Curso desarrollo web sass",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss sass/main.scss estilos/estilos.css --output-style compressed",
"watch-css": "nodemon -e scss -x \"npm run build-css\""
  },
  "author": "Nicolas Menicillo",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^7.0.3",
    "nodemon": "^2.0.20"
  }
}
